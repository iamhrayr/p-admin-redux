import reducer from './reducers';
import * as workActions from './actions';
import * as workTypes from './types';
import workSagas from './sagas';

export { workActions, workSagas, workTypes };
export default reducer;
